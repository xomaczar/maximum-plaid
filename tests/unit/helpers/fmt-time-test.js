import { fmtTime } from 'dummy/helpers/fmt-time';
import { module, test } from 'qunit';

module('Unit | Helper | fmt time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fmtTime([42]);
  assert.ok(result);
});
